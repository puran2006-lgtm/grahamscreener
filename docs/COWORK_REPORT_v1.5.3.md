# Cowork Report — GrahamScreener v1.5.3

**Date:** 2026-05-10
**Task:** Tame Dependabot + Close Open Major-Version PRs
**Status:** Complete — config updated, docs updated, user action documented

## Problem

Dependabot opened 9 PRs proposing major version bumps (Next 14→16, React 18→19, Tailwind 3→4, ESLint 8→10, TypeScript 5→6, @types/node 20→25, plus GitHub Actions majors). All failed CI. Major bumps routinely break builds and require migration work — auto-PRing them creates noise.

## Solution

Updated `.github/dependabot.yml` to ignore all major version bumps (`version-update:semver-major`) for both npm and GitHub Actions. Patch and minor updates continue auto-grouping weekly.

## Files modified (4)

| File | Change |
|---|---|
| `.github/dependabot.yml` | Replaced with new config: ignore majors, separate patch/minor groups, PR limits, ACST schedule |
| `docs/14_AUTOMATION.md` | Updated Dependabot section + added "How to manually upgrade a major version" guide |
| `docs/11_CHANGELOG.md` | Added v1.5.3 entry |
| `DECISIONS.md` | Added v1.5.3 rationale |

## Files created (1)

| File | Purpose |
|---|---|
| `docs/COWORK_REPORT_v1.5.3.md` | This file |

## Verification

| Check | Result |
|---|---|
| `dependabot.yml` YAML parse | Valid |
| `tsc --noEmit` | Clean (0 errors) |

## User Action Required

Close all 9 open Dependabot PRs in one shot using the GitHub CLI:

```bash
# Install gh CLI if needed: https://cli.github.com
# Make sure you're authenticated: gh auth login

# Close all open Dependabot PRs at once
for pr in $(gh pr list --author "app/dependabot" --json number --jq '.[].number'); do
  gh pr close "$pr" --comment "Major version bump — closing per new Dependabot policy. Will revisit when Next/React/Tailwind ecosystem stabilizes."
done
```

After running this command:
- All 9 PRs are closed with explanation comments
- The new `dependabot.yml` prevents major-version PRs from reopening on Monday's schedule
- Patch + minor PRs will continue working normally

---

Generated by Claude Cowork
