# Cowork Report — GrahamScreener v1.5.2

**Date:** 2026-05-10
**Task:** Fix Vercel Cron Frequency for Hobby Tier
**Status:** Complete — cron migrated to GitHub Actions, vercel.json clean

## Problem

Vercel Hobby (free tier) limits cron jobs to daily execution. The `vercel.json` had an hourly schedule (`0 */1 * * *`) which blocked deploy with: "Hobby accounts are limited to daily cron jobs."

## Solution

Migrated the hourly alert checker from Vercel Cron to GitHub Actions. The `/api/cron/check-alerts` endpoint is unchanged — only the caller changed.

## Files created (2)

| File | Purpose |
|---|---|
| `.github/workflows/check-alerts.yml` | Hourly curl to live check-alerts endpoint |
| `docs/COWORK_REPORT_v1.5.2.md` | This file |

## Files modified (5)

| File | Change |
|---|---|
| `vercel.json` | Removed `crons` array entirely |
| `docs/13_ALERTS.md` | Updated to reference GitHub Actions instead of Vercel Cron; added Vercel Pro migration path |
| `docs/14_AUTOMATION.md` | Added check-alerts workflow section and usage table row |
| `docs/12_DEPLOY.md` | Added note about vercel.json no longer having crons |
| `docs/11_CHANGELOG.md` | Added v1.5.2 entry |
| `DECISIONS.md` | Added v1.5.2 rationale for cron migration |

## Verification

| Check | Result |
|---|---|
| `tsc --noEmit` | Clean (0 errors) |
| YAML parse (check-alerts.yml) | Valid |
| vercel.json parse | Valid (no crons key) |

## Required secret

`CRON_SECRET` must be added to GitHub repository secrets (same value as in Vercel env vars). This was already listed in the v1.5 setup but is now used by the check-alerts workflow in addition to the endpoint itself.

---

Generated by Claude Cowork
